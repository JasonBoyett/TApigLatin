#include <string>
using std::string;

bool isLetter(char character);

bool isCapital(char letter);

bool isVowl(char letter);

int findFirstVowl(string word);

bool isApostrophy(char character);

int findLetter(string word, char letter);

string resetCaps(string word);

bool isAllCaps(string word);